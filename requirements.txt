# Web framework
fastapi>=0.115
uvicorn[standard]>=0.32

# HTTP client (MCP shim → FastAPI proxy)
httpx>=0.28

# OpenAI-compatible client (used against Z.AI /paas/v4 with X-Title hack)
openai>=1.50

# Data validation
pydantic>=2.9

# Stealth browser
camoufox[geoip]>=0.4

# MCP SDK (host-side shim)
mcp>=1.2

# Image handling (tile screenshots, upscaling for better VLM accuracy)
Pillow>=10.4

# YAML reading for config files
PyYAML>=6.0
