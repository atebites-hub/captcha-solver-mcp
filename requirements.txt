# Web framework
fastapi>=0.115
uvicorn[standard]>=0.32

# HTTP client (for MCP shim → FastAPI proxy, and for OpenRouter fallbacks)
httpx>=0.28

# OpenAI-compatible client (used to reach OpenRouter)
openai>=1.50

# Data validation
pydantic>=2.9

# Stealth browser
camoufox[geoip]>=0.4

# MCP SDK (host-side shim + any future in-container MCP use)
mcp>=1.2

# Image handling
Pillow>=10.4

# Optional YAML reading (install.sh does the merging via hermes venv, but
# handy for server-side config in future)
PyYAML>=6.0
